# Required: Anthropic API key, server-side only. Never expose this to the client.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for chat responses.
# Default if unset: claude-sonnet-5
# Higher-capability alternative: claude-opus-5
CLAUDE_MODEL_ID=
